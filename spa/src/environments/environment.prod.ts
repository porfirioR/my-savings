export const environment = {
  production: true,
  apiUrl: '/api',
  // TODO: fill in with your production Supabase project's URL and anon (public) key -
  // never the service_role/secret key, that one must stay server-side only.
  supabaseUrl: 'REPLACE_WITH_PROD_SUPABASE_URL',
  supabaseAnonKey: 'REPLACE_WITH_PROD_SUPABASE_ANON_KEY',
};
