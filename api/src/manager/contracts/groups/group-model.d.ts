export declare class GroupModel {
    id: string;
    name: string;
    startMonth: number;
    startYear: number;
    totalRuedas: number;
    createdAt: string;
    updatedAt: string;
    constructor(id: string, name: string, startMonth: number, startYear: number, totalRuedas: number, createdAt: string, updatedAt: string);
}
